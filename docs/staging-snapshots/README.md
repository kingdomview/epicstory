# Staging content snapshots

Pre-change captures of Staging content, so nothing provisional is lost when the epicstory
theme's editable blocks replace older hand-coded content. Reference only — not loaded by the
theme.

- `page-356-home-preexisting.html` — the Home page (ID 356) `post_content` as it existed when
  the epicstory theme was first activated on Staging (2026-09-08). It was a single Custom HTML
  (`wp:html`) block using the old `hw-` classes and HeroWithin copy — captured before any
  homepage reattach so the provisional copy can be recovered or compared against the approved
  epicstory homepage.
